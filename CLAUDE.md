## Graphify Usage Guidelines
- DO NOT read `graph.json`, `GRAPH_REPORT.md`, or `graph.html` directly into context.
- To understand repository structure, dependencies, or map file connections, use the command: `graphify query "<your question>"`